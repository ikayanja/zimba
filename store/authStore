import { create } from "zustand";
import type { AppUser as User } from "../services/supabase/config";

type AuthState = {
  user: User | null;
  role: "homeowner" | "mason" | null;
  setUser: (user: User | null, role?: "homeowner" | "mason") => void;
};

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  role: null,
  setUser: (user, role) => set({ user, role }),
}));
